{
  "name": "MERN-chatrooms",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "kill-port 3000 && kill-port 3001 && kill-port 9229 && concurrently \"cd server && nodemon server\" \"cd client && yarn start\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^4.1.0",
    "kill-port": "^1.3.2"
  }
}
